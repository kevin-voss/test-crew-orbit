# Sprites

Sprites are named by national dex number:

- `1.png` — normal sprite for #1 (Bulbasaur)
- `1-shiny.png` — shiny sprite for #1

Regenerate assets and `data/pokemon.json` from PokeAPI:

```bash
node scripts/generate-pokemon-data.mjs
```
